# Shader correctness fixes

- Sampled images now multiply straight sampled RGB by `sampleColor.a * opacity` before premultiplied source-over blending.
- The Vulkan image fixture keeps the original transparent half-alpha assertion and adds a separate non-black opaque destination assertion. Native nearest and linear routes pass. Nearest digest is `7118837291919526053`; linear digest is `505875462623902649`; managed record allocation is zero; resource plateau, retirement, and rehydration pass; Vulkan validation errors are zero.
- Canonical ShaderGen `generate` and `check` pass with Slang 2026.16, shaderc 2026.3, and SPIRV-Tools 2026.3. Generated and production sampled-image SPIR-V match at SHA-256 `f06f2d9e35eda71a018575beaa630c876960477bc9fcde2c45456bf0e47da694`. Both manifests match at SHA-256 `a51b2705bc7a50f6bab5fab77a9c8e1bcdb237403ead47b9ac5c82958e0a8556`.
- Liquid glass now derives the dispersed sample coverage alpha from the maximum of the red, center, and blue sample alphas. This keeps each premultiplied dispersed color channel bounded by output alpha and retains intentional dispersion.
- Actual native alpha-boundary readback changed x34 from `129/23/27/255` to `129/10/7/255`. All 32 opaque control samples stayed byte-identical at `247/23/7/255`. All 21 transparent control samples stayed byte-identical at `0/0/0/199`. Both runs passed Vulkan diagnostic and resource cleanup gates.
- The liquid regression fixture uses the real Gallery shader source, ShaderEffect build adapter, backdrop capture, and Vulkan window readback. It asserts the boundary, transparent side, and full opaque control row with tolerances. A fresh identical-runtime bundle swap kept the application DLL, PDB, and Goo.dll byte-identical. The original bundle failed the edge assertion with `198/23/11/255` and exit 134. The fixed bundle passed with `172/17/7/255`, discontinuity `129/10/7/255`, transparent `0/0/0/199`, opaque `247/23/7/255`, validation zero, cleanup one, and exit zero.
- Focused strict gslint passes with repository exclusions `GL0005=none` and `GL0006=none`. Focused diff check passes. AsyncReadback and VulkanProof Release builds pass with zero warnings and errors.
- The first liquid A/B used different Goo.dll hashes because coordinated drag/drop corrections landed between frozen builds. The final causality run removes that limitation by swapping only the liquid effect bundle between otherwise byte-identical fresh runtime copies.
