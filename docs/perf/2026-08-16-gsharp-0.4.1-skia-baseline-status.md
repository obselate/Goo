# G# 0.4.1 Skia Comparison Reference Status

Date: 2026-08-16

Branch: `gaps-and-reductions`

Status: historical, nongating archive

This document preserves pre-Vulkan measurements only. Vulkan and Skia are not treated as comparable.
Nothing here is a control, denominator, threshold, regression reference, or release gate.

## Historical floor retained

| Evidence | SHA-256 | Use |
|---|---|---|
| `docs/perf/RELEASE-BASELINE.md` | `e9900802d0b6723752862d29c13414cb984bd3c3dbec58cfc2f30c6cbbc221fe` | Older workload and allocation evidence |
| `docs/perf/2026-08-07-skia4-premigration-baseline.md` | `c92d209f2224552362ffa253ffa455c3eae22874ad30238f3b212fc7828071bc` | SkiaSharp 4.151.1 and G# 0.3.633 historical floor |

These files remain immutable historical evidence. They are not relabeled as G# 0.4.1 results.

## Current G# 0.4.1 evidence

The candidate migration was verified with the exact official SDK package:

- Release build with warnings as errors: pass, zero warnings and zero errors.
- Existing suite: 498 passed, zero failed, zero skipped.
- Package creation: pass.
- Fresh NuGet cache package consumer: pass.
- README package consumer: pass, zero warnings and zero errors.
- Package smoke executable: pass, including mounted external `Cell[TInput]`, typed `Build(input)`,
  imported `ShouldRebuild`, native SDL open, pump, close.
- Linux x64 NativeAOT package consumer: pass.
- Generated API documentation: regenerated from the canonical 0.4.1 XML output.

| Artifact | Bytes | SHA-256 |
|---|---:|---|
| Temporary `Goo.0.2.0.nupkg` | 1,143,030 | `736485d31f8349e39b470ca4848892671ea855dbdd07db4167ab7eba14228cc5` |
| Framework-dependent `Goo.dll` | 695,808 | `911f91aafc23090c4feace90fcc0dae657fd3610380c72f1a8708669854001a5` |
| Framework-dependent package smoke | 6,656 | `146b55cbf1b2c0daefa052f9466089317208fe9d3df645f872c3f1a0eca9f225` |
| NativeAOT package smoke | 4,219,240 | `d5bbe64cde6bee7c54eed2a295d7e798e37476628d3905b3fe9bd80b9bcbb245` |
| Packaged SDL 3.4.0 | 1,578,808 | `eabae60185dd3b6dfc832373069cf5b55243bacd4e608bf72e9afea4354c4aff` |

The temporary Linux package contained 15 entries and 2,905,436 uncompressed bytes. Its relevant
payload was:

| Payload | Bytes |
|---|---:|
| `Goo.dll` | 695,808 |
| `Goo.InternalTextInterop.dll` | 124,928 |
| `Yoga.Net.dll` | 126,464 |
| `Goo.xml` | 135,800 |
| `libSDL3.so` | 1,578,808 |

The fresh framework-dependent consumer directory was approximately 20 MiB. Its largest runtime
files were:

| Payload | Bytes |
|---|---:|
| `libSkiaSharp.so` | 11,783,800 |
| `libHarfBuzzSharp.so` | 2,935,200 |
| `Hexa.NET.SDL3.dll` | 2,525,184 |
| `libSDL3.so` | 1,578,808 |
| `Goo.dll` | 695,808 |
| `SkiaSharp.dll` | 527,160 |

The package and consumer were created under temporary directories and deleted after inspection.
Their hashes are recorded execution evidence for S03, not baseline identities. No qualifying S04
manifest or new artifact set is required.

## Recorded measurements

The recorded Q10 text lane used the normal Goo Release project and public Goo APIs. It measured the
revision 2 exact 1 MiB text workload through the shared strict protocol: five isolated processes,
300 warmups, and 2,000 measured samples per process. The recorded Release builds passed with zero
warnings and zero errors.

The local rootless Weston 15 scale-1 hardware-backed RTX 3080 GL path works. The clean-source Linux
x64 NativeAOT production binary publishes at 6,267,440 bytes with SHA-256
`8116bf0cce3935ba2999e85a40294d9b0cc6faf96f97e8868569ef67fba63199`.

A source-dirty AOT diagnostic batch passed with these pooled results:

| Metric | p50 | p95 | p99 | p99.9 | Worst |
|---|---:|---:|---:|---:|---:|
| Total microseconds | 281.921 | 445.58 | 671.987 | 1877.671 | 2024.147 |
| Managed allocation bytes | 88,008 |  |  | 95,760 | 120,336 |

This is recorded nonqualifying evidence. The source was dirty, complete S04 metrics and visual
evidence were not captured, and the Windows and Linux integrated-GPU legs are missing. It remains
historical comparison information only.

A clean-source local NativeAOT batch also passed at source commit
`9d285338575da99a4f26f059311aa783c2e0b742` with `sourceDirty=false`:

| Metric | p50 | p95 | p99 | p99.9 | Worst |
|---|---:|---:|---:|---:|---:|
| Total microseconds | 284.267 | 461.491 | 694.229 | 1939.289 | 2090.614 |
| Managed allocation bytes | 88,008 | 88,008 | 88,008 | 95,760 | 120,336 |

The strict protocol produced five raw artifacts and 10,000 pooled samples. Its content-derived
identity is `goo-gsharp-q10-text-editing-r2-fc1d3a593d7b7df8`, its baseline key is
`goo-gsharp.q10.text-editing.r2.ecb313097dfbd62f`, and its canonical content hash is
`13313aa9db874ab5f9720b186347adfd104354c8f204c74adb93817435d65511`.

The Ubuntu 22.04 compatibility lane was also provisioned locally with .NET 10, SDL 3.4.0, Weston 9,
Mesa, and the fixed Inter font. Its five-process NativeAOT batch passed under headless llvmpipe:

| Metric | p50 | p95 | p99 | p99.9 | Worst |
|---|---:|---:|---:|---:|---:|
| Total microseconds | 2255.273 | 2540.881 | 2775.383 | 3059.840 | 3464.072 |
| Managed allocation bytes | 88,080 | 88,080 | 88,080 | 95,832 | 120,408 |

The Ubuntu result proves binary and runtime compatibility. It is not a performance baseline because
it used software rendering and the copied source snapshot did not expose Git provenance.

The clean local result is recorded comparison information. It covers one of eight workloads and
does not include complete stage metrics, memory, lifecycle, visual, package, display, driver,
Windows, or integrated-GPU evidence. No additional S04 capture is required.

## Recorded implementation notes

The recorded clean result used protocol v1. Its recorded metadata included distinct child process IDs,
fixed Q10 gate records, provenance fields, build-sidecar evidence, visual and package artifact
references, source artifact references, and validation error count. These records are historical only.
The S04 qualification parser and manifest system were removed after recording.

The recorded empty `Window` check measured 3,352 bytes instead of 4,728 bytes, a reduction of 1,376
bytes per window. The temporary frame trace capture used a bounded preallocated buffer and flush-only
NDJSON with stable stage, window, and frame IDs. It added no public API and was removed after
recording.

A source-dirty NativeAOT v2 verification binary was 6,346,944 bytes with SHA-256
`b74c44bac5a65e4cc348e5090e800962fdbc0c0b232ad273a57ddf11fc92a17a`. The production text workload
completed 2,000 measured frames and emitted 28,000 stage records with zero dropped records. All 2,000
frame records were rendered. This is retained diagnostic evidence only, not a qualifying S04 result.
The temporary serializer and trace path used for this recording were removed after recording.

The recorded validation run passed 498 of 498 tests after the intentional allocation-floor reduction.
The temporary benchmark builds reported zero warnings and zero errors before their removal.

The historical evidence above remains immutable. It is not relabeled as G# 0.4.1 evidence.

## Next action

No additional S04 evidence capture is required. The recorded measurements above are accepted as the
comparison reference. The benchmark, provenance, protocol, and trace implementation created for
these recordings was removed after recording, so no permanent S04 infrastructure remains. Proceed
to S05 and S06. Do not change or delete the Skia baseline implementation before the S18 cutover
gates pass.
